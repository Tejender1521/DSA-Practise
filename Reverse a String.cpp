#include<bits/stdc++.h>

using namespace std;

int main()
{
    METHOD 1;
    reverse(str.begin(),str.end());
    


    METHOD 2;
    string s = "";
    for(int i = str.length()-1; i>=0 ; i--)
    {
        s += str[i];
    }
    return s;
}